# Application Agent 自动发现入口

本文件用于 Agent 工具自动发现 `Application/` 作用域规则。完整且唯一的总指导位于
[`agent.md`](agent.md)，开始任何 Application 任务前必须完整阅读并遵守该文件。

不要在本页复制 `agent.md` 的架构、资源、禁止项、验证或交接规则；规则变化只修改
`agent.md`，本页始终保持为兼容入口。
